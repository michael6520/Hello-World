#pragma once

#include <iostream>
#include <string>
#include <thread>
#include <vector>

struct _ {
    std::vector<std::string> __;
    std::vector<char> ___;
    size_t ____ = 0;
};

void _____(_& __);

void ______(const _& __);